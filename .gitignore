# OAuth secrets and cached tokens — never commit these
credentials.json
token.json
*.token.json

# Built binary (Go)
/gmail-ftp
/dist/

# Rust build artifacts
/target/

# Editor / OS noise
*.swp
.DS_Store
