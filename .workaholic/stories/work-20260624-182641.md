# Branch Story: work-20260624-182641

## 1. Overview

A small docs/process change on top of the qfs `main`: promote the qfs README to the repository
root (this monorepo has a single package, so a pointer README at the root was just indirection),
add a `CLAUDE.md`, and establish + apply the per-PR patch-version rule (`0.0.1 → 0.0.2`).

Base: `main`. First change to run through the newly-defined branch → PR → report → ship → bump
workflow.

## 2. Changes

- **README at the root.** `packages/qfs/README.md` → `README.md`, with relative links fixed for the
  new location (`docs/…`, `packages/qfs/crates/…`, the build command now `cd packages/qfs && …`).
  Dropped the old monorepo-pointer README. The `.workaholic/RFDs/…` link, previously broken from
  the package dir, now resolves.
- **`CLAUDE.md`.** Documents build/test commands, the anti-drift docs rule, the versioning rule, and
  a `## Deploy` section (deploy = cut the `v0.0.x` release tag).
- **Versioning rule.** Every shipped PR bumps the qfs crate patch version; applied here `0.0.1 →
  0.0.2`. Recorded in `CLAUDE.md` (and session memory).
- **Credential gate.** `scripts/check-no-live-credentials.sh` now scans `../README.md` (root).

## 3. Outcome

- `qfs --version` → `0.0.2`. Build, `cargo fmt --check`, clippy, and the anti-drift
  `gen-docs --check` all clean; the credential gate passes against the relocated README.
- No code/grammar/registry change — docs + process + version string only.

## 4. Concerns

None. Documentation and version-string only; the binary's behavior is unchanged from v0.0.1.

## 5. Release readiness

**Safe to ship.** Docs/process change with a patch bump; fully green. On ship, cut `v0.0.2` so the
published release and `qfs --version` match.
