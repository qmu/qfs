---
type: Story
branch: work-20260818-224038
tickets_completed: 0
mission: [the-current-situation-of-qfs-is-documented-as-it-actually-stands]
tickets: []
---

## Handoff

**This branch is unfinished. Someone must continue it.**

- **Done:** The blocker on `20260817131540-file-the-bun-plgg-md-parse-defect-upstream` was re-checked against live sources and the result recorded on the ticket with today's date. The paste-ready upstream report in the ticket body is confirmed still accurate as written.
- **Not done:** Step 1 — open the issue in `qmu/plgg` carrying the construct, the two-runtime table and the affected versions. Steps 2-4 follow from it and stay queued. The ticket stays in `todo/`.
- **Next step:** A person or session with write access to `qmu/plgg` files the issue (the body is ready to paste from the ticket), then records the issue URL into `packages/qfs-viewer/scripts/smoke-npx.sh`'s exemption in place of the ticket number.
- **Attempted:** `npm view plgg-md versions --json` → `["0.0.1","0.0.2","0.0.3"]` with `dist-tags.latest = 0.0.3`, so no fixed release exists and steps 3-4 remain blocked on the upstream publish. `which deno` → not found, so deno's leg of the runtime matrix stays unproven rather than broken. The cross-repository write was not attempted: this session's GitHub access is stated as scoped to `qmu/qfs` alone, and issuing a call the runner is instructed not to make is not evidence-gathering.

## 1. Overview

The mission's three acceptance items are all met; what is left in its queue is one follow-up that cannot be done from inside this repository. This branch does the only thing an unattended run can honestly add to it: date the blocker, with the commands behind that judgement, so a wait is distinguishable from a skip.

**Highlights:**

1. The upstream `plgg-md` defect is confirmed unfixed — `0.0.3` is still the newest published version, so the ticket's "no version bump fixes it" claim and the `smoke-npx.sh` exemption that rests on it both stand.
2. deno is still absent from the container, so the matrix gap the ticket names in its Considerations is still unproven rather than closed.
3. The ticket stays queued and blocked on reach into `qmu/plgg`, with this run's checks recorded on it.

## 2. Motivation

`20260817131540` was recorded blocked on 2026-08-17 because filing the defect needs write access to the private `qmu/plgg` repository, which an unattended runner scoped to `qmu/qfs` does not have. That blocker has two halves with different lifetimes: the access half cannot change from here, but the half that waits on someone publishing a fixed `plgg-md` can change at any moment without anybody in this repository noticing. A ticket that says "blocked" with a month-old date does not distinguish "still true" from "nobody looked", and the consumer-side exemption in `smoke-npx.sh` makes a factual claim — that `0.0.3` is the newest published version — which quietly becomes false the day a fix ships.

## 3. Changes

### 3-1. Date the upstream plgg-md filing's blocker ([d802c45](https://github.com/qmu/qfs/commit/d802c45))

The ticket gains a `## Still blocked — 2026-08-18` section: a table of the three checks actually run this time, their commands and their results, and an explicit statement that the paste-ready report body has not gone stale.

## 4. Outcome

Nothing a user of `qfs` experiences changes. What changes is that the ticket's wait is now evidenced rather than assumed: a reader can see which parts of it are waiting on an upstream publish (checked today) and which on an access grant, without re-running anything.

## 5. Concerns

### The upstream filing has no owner inside this repository

- **Severity:** moderate
- **Description:** `plgg-md`'s published dist cannot be parsed by bun, so `npx`-installing any plgg-family package is broken under bun. The fix is a build-side change in `qmu/plgg`, and every runner that reaches this ticket is scoped to `qmu/qfs`, so the queue cannot advance it however often it is claimed.
- **How to Fix:** Someone with write access to `qmu/plgg` files the issue from the ticket's ready-to-paste body, or the ticket is moved to the icebox as developer-deferred work. The `smoke-npx.sh` exemption's 2026-11-17 revisit date is the backstop either way.

### Re-checking on every tick would turn the ticket into a diary

- **Severity:** low
- **Description:** This re-check is worth recording once. Repeating it on each unattended tick would append a near-identical section every hour and bury the finding it is attached to.
- **How to Fix:** The claim is left standing and this PR left open rather than merged, so the survey stops re-offering the unit until a person acts on it.

## 6. Notes

The mission `the-current-situation-of-qfs-is-documented-as-it-actually-stands` has all three `## Acceptance` items checked. Its remaining queue is this one follow-up, minted while driving an earlier ticket. Whether the mission closes with the follow-up still queued is a developer's call, not the run's.
