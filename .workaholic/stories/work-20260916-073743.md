---
type: Story
branch: work-20260916-073743
title: Verify the complete Codex skill catalog
description: Verify all 14 shared QFS skills load from an isolated Codex installation and document the complete catalog.
tickets_completed: 1
mission: []
tickets: [20260907183404-verify-codex-plugin-distribution-and-drift.md]
---

## 1. Overview

Verified QFS discovery in an isolated Codex installation and corrected the installation guide to
enumerate all 14 shared skills. Added regression coverage for a missing marketplace registration.

**Highlights:**

1. Codex CLI 0.154.0 loads all 14 skills from plugin cache 0.22.4 without loading errors.
2. The guide lists the full registered catalog, replacing the incomplete two-skill list.
3. A missing base-skill registration produces a marketplace-path-specific test failure.

## 2. Motivation

An enabled installation record did not establish that Codex loaded the complete plugin independently
of repository-local links. The earlier implementation established the shared distribution checker
and CI integration; this follow-up strengthens host-loading evidence and closes the remaining
registration-test gap. The queued copy of the already-implemented ticket is now archived.

## 3. Changes

### 3-1. Verify Codex plugin distribution ([28a2a03](https://github.com/qmu/qfs/commit/28a2a03))

A fresh app-server catalog from an empty project confirms every expected skill is enabled and loaded
from the installed cache. The guide records the observed host version and full catalog, and the
checker fixtures now cover an omitted base-skill registration explicitly.

## 4. Outcome

QFS advances to 0.0.134 after integrating main at d0e49cc; the shared plugin remains at 0.22.4.
The integration preserves the newer mounted-write and Slack fixes. The plugin checker, all 12
negative fixtures, exact 14-skill catalog comparison, generation checks and docs build passed again.
Earlier isolated host discovery, 2801 workspace tests, 529 serialized library tests, clippy and
formatting remain recorded in the ticket; those full suites were not rerun for this documentation
and fixture recovery. Exact-head CI is required before delivery.

## 5. Successful Development Patterns

- An empty project and a fresh app-server distinguish installed-plugin discovery from repository-local links.

## 6. Notes

The host `/tmp` was full. Unchanged Rust suites passed inside a private mount namespace with a
dedicated `/tmp`; neither the host mount nor other users' files changed. No release tag was pushed.

## Merge Outcome

merge_refused: checks_pending

The prior conflict was resolved by regenerating the story index and allocating 0.0.134 above main.
The next delivery attempt must read checks for the newly published head.

## Notification Status

PR #118 remains unmerged. Completion notification text is not composed until merge evidence is
confirmed. Slack delivery is held because the originating thread is unresolved; no message was sent.
