---
created_at: 2026-07-16T14:48:16+09:00
author: a@qmu.jp
type: housekeeping
layer: [Infrastructure]
effort:
commit_hash:
category:
depends_on:
mission: declared-drivers-are-the-normal-way-to-add-a-service
---

# RESUME: report and ship `work-20260715-205333`, then drive the re-homing on a fresh branch

## Overview

**Do this ticket first — it is the `/report` step, not a build.** The session that wrote it ended
by owner instruction (`/carry`) at exactly the point where the branch is ready to become a PR.
Nothing is half-implemented; the tree is clean; both implementations on the branch shipped with
their quality gates proven in both directions.

**Branch position** (`work-20260715-205333`, 12 commits past `e4dc66e`, tree clean):

- `0afaf2b` — **splitter unification**: one lexer-backed `.qfs` splitter in
  `core/src/ddl/document.rs`; both hand-rolled scanners deleted; `;` added to
  `is_path_delimiter` (`lex.rs`) — that one line also fixed a shipped language bug
  (`transaction { … /path; … }` parse-erroring). Patch bumped to **0.0.72**, plugin versions to
  **0.11.9** (hard break: a bare path can no longer carry a literal `;`).
- `3bc2710` — **replace-on-install**: `insert_driver` deletes the same-`(kind,name,verb)` row in
  its audited transaction (supersede, ungated); reads resolve newest-per-key so append-era
  registries heal without re-install. Both new tests were run against unfixed code first and
  failed as predicted.
- Mission `declared-drivers` is **2/7** with a ruled dependency spine; gates on both main-tree
  missions were deliberately demoted to none (`3e21d03`, owner directive — north star in
  `gate_assert`, verification lives in ticket gates).
- `833dd6b` — ticket `20260716143641-rehome-declarative-tables-into-the-system-db.md` sits in
  todo: **item 6 ruling (owner choice C)** — `path_binding` + `connection_consent` move to the
  System DB, Project DB becomes the vault proper. It explicitly wants **its own branch after this
  one ships**; do not drive it on `work-20260715-205333`.

## Implementation Steps

1. **`/report` this branch.** Inputs the report needs:
   - Concern `the-config-comment-stripper-truncates-paths` → **resolved by `0afaf2b`**. Its repro
     line is stale (`qfs run -f` — no `-f` exists; `qfs run` takes one statement).
   - Concern `duplicate-declaration-rows-still-resolve-oldest` → **resolved by `3bc2710`**. Its
     `severity: low` is contradicted by measurement (the operator's live `/chatwork` was running a
     superseded view body under a newer type contract); re-rate when judging.
   - Concern `project-db-configuration-events-are-not` → **NOT resolved**; its How-to-Fix is
     **superseded** by the item-6 ruling — judge it against ticket `20260716143641`, not its own
     text.
   - Write deferred-concern verdicts to the **session scratchpad**, not
     `/tmp/deferred-concern-verdicts.json` (shared-host collision).
2. **`/ship`**: merge, tag `v0.0.72`, deploy per `CLAUDE.md ## Deploy`. Delete any
   `<pr>-carried-*` duplicate concern files after ship (known /ship extraction bug).
3. **Then `/drive`** picks up `20260716143641` (re-homing) on a **fresh branch off updated main**.
   Mission items 1 (cloud account declarations) and 3 (sql/git → path_binding) are ruled to wait
   behind it — both write `path_binding`.

## Standing state a fresh session must know

- **Live behavior change pending owner-attended verification**: after `3bc2710`, `/chatwork` on
  this box resolves the *newer* view body (14 append-era duplicate rows remain on disk, correctly
  ignored; physical cleanup awaits an uninstall surface — deliberate non-goal).
- **`QFS_HOME` does not exist.** Isolate qfs experiments with `XDG_CONFIG_HOME` (the binary's own
  tests do); trusting `QFS_HOME` polluted the real System DB this session (rows deleted via
  sqlite3 with owner approval; backup at `~/tmp-cargo/system.db.bak-before-dupdemo-cleanup`).
  This lesson could NOT be saved to agent memory — the repo-confinement guard blocks writes to
  the memory directory (unreported workaholic issue, see below).
- **Claude-mission worktree** `.worktrees/claude-code-sessions-are-queryable-and-steerable-as-qfs-paths`
  (branch `work-20260716-015007`): mission committed (0/7, gate `live-app`), **no kickoff tickets
  yet**. Natural first tickets: mount registration + `e2e_cli.rs:356` correction (one line
  unlocks reads), path canon (`/hosts/<host>/claude` ruled canonical), real JSONL reader behind
  `SessionSource`; session-launch needs a design brief first. Its live-app gate cannot resolve
  ports until the filed `gate.sh` fix ships in workaholic.
- **Four tickets filed into `qmu/workaholic`** (todo, uncommitted there by design): unassigned
  missions in summary, `gate.sh` worktree port resolution, `check-worktrees.sh` dropping the last
  worktree (its guard silently never fires for a single worktree — affects /drive/ /ticket Phase 0
  here too), and the mission-gate design report. A fifth candidate is unreported: the
  repo-confinement guard blocks the agent's own memory writes.
- Shared tree: other sessions run concurrently — stage explicitly via commit.sh file args, never
  `git add -A`. Disk was reclaimed (qfs-legacy checkout removed, owner-approved; history lives at
  `qmu/qfs-legacy` remote, one local-only commit `330f28b` was knowingly lost).
- Model note: this session ran design rulings on Fable per policy; implementation drives can run
  on Opus or cheaper.

## Policies

- `workaholic:implementation` / `objective-documentation` — this handoff records only verified
  positions (commit hashes, test outcomes, filed paths), no aspirations.
- `workaholic:implementation` / `operational-planning` — shaped as a recovery checkpoint: the next
  session's first action is deterministic (`/report`), with the judgment inputs it cannot
  reconstruct cheaply (concern verdicts, supersessions) written down.

## Quality Gate

- The resuming session runs `/report` on `work-20260715-205333` **before** driving any code
  ticket, and hands the three concern verdicts above to the report flow unchanged unless the
  source contradicts them.
- The re-homing ticket is driven only after this branch ships, on a fresh branch.
- This ticket archives with the `/report`/`/ship` cycle it describes — it does not survive as
  standing documentation.
