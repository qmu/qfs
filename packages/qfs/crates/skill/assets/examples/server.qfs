-- server (/server self-config). DESCRIBE /server/triggers -> relational binding table.
-- Step 2: CREATE TRIGGER desugars to one /server/triggers config write (PREVIEW-as-CI-test).
CREATE TRIGGER notify ON inbox DO INSERT INTO /log VALUES ('mail arrived')
