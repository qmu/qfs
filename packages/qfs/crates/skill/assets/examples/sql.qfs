-- sql (relational_table, pushdown). DESCRIBE /sql/pg/orders -> pushdown where+project.
-- Step 2: a pure read; WHERE + projection push down to Postgres. No COMMIT.
/sql/pg/orders |> WHERE total > 100 |> SELECT id, total
